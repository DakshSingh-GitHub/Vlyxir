{
    "id": "lfu_cache",
    "title": "LFU Cache",
    "description": "Design and implement a data structure for a Least Frequently Used (LFU) cache.\n\nImplement the LFUCache class:\n- LFUCache(int capacity) Initializes the object with the capacity of the data structure.\n- int get(int key) Gets the value of the key if the key exists in the cache. Otherwise, returns -1.\n- void put(int key, int value) Update the value of the key if present, or inserts the key if not already present. When the cache reaches its capacity, it should invalidate and remove the least frequently used key before inserting a new item. For this problem, when there is a tie (i.e., two or more keys with the same frequency), the least recently used key would be invalidated.",
    "input_format": "The first line contains a list of method names. The second line contains a list of corresponding arguments.",
    "output_format": "A list of values separated by space (null for void methods).",
    "constraints": [
        "0 <= capacity <= 10^4",
        "0 <= key <= 10^5",
        "0 <= value <= 10^9",
        "At most 2 * 10^5 calls will be made to get and put."
    ],
    "difficulty": "hard",
    "judge_mode": "str_compare_strip",
    "time_limit": 1,
    "sample_test_cases": [
        {
            "input": "LFUCache put put get put get get put get get get\n2,1 1,2 2,1,3 3,2,3,4 4,1,3,4",
            "output": "null null null 1 null -1 3 null -1 3 4"
        }
    ],
    "hidden_test_cases": [
        {
            "input": "LFUCache put get\n0,1 1,1",
            "output": "null null -1"
        }
    ]
}
